---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: nodescans.clamav.io
spec:
  group: clamav.io
  names:
    kind: NodeScan
    listKind: NodeScanList
    plural: nodescans
    shortNames:
    - ns
    - nodescan
    singular: nodescan
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.nodeName
      name: Node
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.filesScanned
      name: Scanned
      type: integer
    - jsonPath: .status.filesInfected
      name: Infected
      type: integer
    - jsonPath: .status.duration
      name: Duration
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodeScan is the Schema for the nodescans API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeScanSpec defines the desired state of NodeScan
            properties:
              excludePatterns:
                description: ExcludePatterns are regex patterns for paths to exclude
                items:
                  type: string
                type: array
              fileTimeout:
                default: 300000
                description: FileTimeout in milliseconds for scanning each file
                format: int64
                type: integer
              forceFullScan:
                description: ForceFullScan forces a full scan even if incremental
                  is enabled
                type: boolean
              incrementalConfig:
                description: IncrementalConfig configures incremental scan behavior
                properties:
                  baselineInterval:
                    default: 7
                    description: |-
                      BaselineInterval triggers a full scan every X incremental scans.
                      For example, if set to 7, a full scan is triggered every 7 incremental scans
                    format: int32
                    maximum: 30
                    minimum: 1
                    type: integer
                  cacheExpiration:
                    default: 168
                    description: |-
                      CacheExpiration defines the cache validity duration (in hours).
                      After this period a full scan is forced
                    format: int32
                    type: integer
                  enabled:
                    default: false
                    description: Enabled activates incremental scanning
                    type: boolean
                  maxAge:
                    default: 24
                    description: |-
                      MaxAge defines the maximum age (in hours) of files to consider for incremental scans.
                      Used with the modified-only and smart strategies
                    format: int32
                    type: integer
                  minTimeBetweenScans:
                    default: 6
                    description: |-
                      MinTimeBetweenScans defines the minimum delay (in hours) between two scans.
                      Prevents rescanning the same node too frequently
                    format: int32
                    type: integer
                  skipUnchangedFiles:
                    default: true
                    description: SkipUnchangedFiles skips files whose mtime+size have
                      not changed since the last scan
                    type: boolean
                  strategy:
                    default: incremental
                    description: Strategy defines the scan strategy
                    enum:
                    - full
                    - incremental
                    - modified-only
                    - smart
                    type: string
                type: object
              maxConcurrent:
                default: 5
                description: MaxConcurrent files to scan in parallel
                format: int32
                maximum: 20
                minimum: 1
                type: integer
              maxFileSize:
                default: 104857600
                description: MaxFileSize in bytes - files larger than this will be
                  skipped
                format: int64
                type: integer
              nodeName:
                description: NodeName is the name of the node to scan
                minLength: 1
                type: string
              paths:
                description: |-
                  Paths to scan on the node
                  If not specified, uses paths from ScanPolicy or defaults
                items:
                  type: string
                type: array
              priority:
                default: medium
                description: |-
                  Priority of the scan (high, medium, low)
                  Affects scheduling and resource allocation
                enum:
                - high
                - medium
                - low
                type: string
              resources:
                description: Resources for the scan job
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              scanPolicy:
                description: |-
                  ScanPolicy references a ScanPolicy to use for this scan
                  If not specified, default scan parameters will be used
                type: string
              strategy:
                allOf:
                - enum:
                  - full
                  - incremental
                  - modified-only
                  - smart
                - enum:
                  - full
                  - incremental
                  - modified-only
                  - smart
                default: full
                description: Strategy defines the scan strategy to use
                type: string
              ttlSecondsAfterFinished:
                default: 86400
                description: |-
                  TTLSecondsAfterFinished limits the lifetime of a Job that has finished
                  execution (either Complete or Failed). If this field is set,
                  ttlSecondsAfterFinished after the Job finishes, it is eligible to be
                  automatically deleted.
                format: int32
                type: integer
            required:
            - nodeName
            type: object
          status:
            description: NodeScanStatus defines the observed state of NodeScan
            properties:
              cacheHitRate:
                description: CacheHitRate is the percentage of files that were skipped
                  (0-100, integer)
                format: int32
                maximum: 100
                minimum: 0
                type: integer
              completionTime:
                description: CompletionTime of the scan
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the NodeScan's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              duration:
                description: Duration of the scan in seconds
                format: int64
                type: integer
              errorCount:
                description: ErrorCount is the number of errors encountered during
                  scan
                format: int64
                type: integer
              exitCode:
                description: |-
                  ExitCode is the exit code returned by the scanner container on failure.
                  0 means the container exited normally (or did not terminate yet).
                format: int32
                type: integer
              failureReason:
                description: |-
                  FailureReason contains the human-readable reason why the scan job failed.
                  Populated from the container's terminationMessage / reason field.
                type: string
              filesInfected:
                description: FilesInfected is the number of infected files found
                format: int64
                type: integer
              filesScanned:
                description: FilesScanned is the total number of files scanned
                format: int64
                type: integer
              filesSkipped:
                description: FilesSkipped is the number of files skipped
                format: int64
                type: integer
              filesSkippedIncremental:
                description: FilesSkippedIncremental is the number of files skipped
                  due to incremental scan
                format: int64
                type: integer
              infectedFiles:
                description: |-
                  InfectedFiles contains details of infected files
                  Limited to the first 100 entries. When more infected files exist,
                  InfectedFilesTruncated is set to true and a warning event is emitted.
                items:
                  description: InfectedFile represents a file found to be infected
                    with malware
                  properties:
                    detectedAt:
                      description: DetectedAt is when the infection was detected
                      format: date-time
                      type: string
                    path:
                      description: Path to the infected file on the node
                      type: string
                    size:
                      description: Size of the infected file in bytes
                      format: int64
                      type: integer
                    viruses:
                      description: Viruses detected in the file
                      items:
                        type: string
                      type: array
                  required:
                  - path
                  - viruses
                  type: object
                type: array
              infectedFilesTruncated:
                description: |-
                  InfectedFilesTruncated is true when the scan detected more than 100 infected
                  files. Only the first 100 are stored in InfectedFiles; the full count is
                  still reflected in FilesInfected. Always check this flag before drawing
                  security conclusions from InfectedFiles.
                type: boolean
              jobRef:
                description: JobRef is a reference to the created Job
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              lastTransitionTime:
                description: LastTransitionTime is the last time the phase transitioned
                format: date-time
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent generation observed by the controller.
                  Used by ArgoCD, Flux and other GitOps tools to detect whether the controller
                  has processed the latest version of the spec.
                format: int64
                type: integer
              phase:
                description: Phase of the scan
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                type: string
              reportPath:
                description: ReportPath is the path to the detailed scan report on
                  the node
                type: string
              resultsPartial:
                description: |-
                  ResultsPartial is true when scan result parsing failed after all retries
                  and the status reflects incomplete data. The scan itself succeeded (the
                  scanner Job exited 0) but the controller could not fully parse the output.
                  Do NOT treat this scan as a clean bill of health — re-run the scan.
                type: boolean
              scannerCPUUserMilliseconds:
                description: |-
                  ScannerCPUUserMilliseconds is the user-space CPU time (milliseconds) consumed
                  by the scanner Node.js process during the scan, as reported by
                  process.cpuUsage(). Stored as int64 milliseconds to avoid float serialization
                  issues in CRD schemas.
                  Note: child process (clamscan) CPU time is not included; use cAdvisor
                  container_cpu_usage_seconds_total for full container CPU accounting.
                format: int64
                type: integer
              scannerMemoryRSSBytes:
                description: |-
                  ScannerMemoryRSSBytes is the RSS memory of the scanner Node.js process at
                  the end of the scan (bytes), as reported by process.memoryUsage().rss.
                  Note: in standalone mode this captures only the Node.js orchestrator; the
                  clamscan child process memory is tracked separately by cAdvisor.
                format: int64
                type: integer
              startTime:
                description: StartTime of the scan
                format: date-time
                type: string
              strategyUsed:
                description: StrategyUsed is the actual strategy that was used for
                  this scan
                enum:
                - full
                - incremental
                - modified-only
                - smart
                type: string
              timeSaved:
                description: TimeSaved is the estimated time saved by incremental
                  scanning (in seconds)
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .spec.nodeName
      name: Node
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.filesScanned
      name: Scanned
      type: integer
    - jsonPath: .status.filesInfected
      name: Infected
      type: integer
    - jsonPath: .status.duration
      name: Duration
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: NodeScan is the Schema for the nodescans API (v1beta1 — stable,
          preferred version)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeScanSpec defines the desired state of NodeScan
            properties:
              excludePatterns:
                description: ExcludePatterns are regex patterns for paths to exclude
                items:
                  type: string
                type: array
              fileTimeout:
                default: 300000
                description: FileTimeout in milliseconds for scanning each file
                format: int64
                type: integer
              forceFullScan:
                description: ForceFullScan forces a full scan even if incremental
                  is enabled
                type: boolean
              incrementalConfig:
                description: IncrementalConfig configures incremental scan behavior
                properties:
                  fullScanInterval:
                    default: 10
                    description: FullScanInterval defines how often a full scan runs
                      when strategy is "smart"
                    format: int32
                    type: integer
                  maxFileAgeHours:
                    default: 24
                    description: MaxFileAgeHours is the maximum age (in hours) of
                      files to consider for incremental scans
                    format: int32
                    type: integer
                  skipUnchangedFiles:
                    default: true
                    description: SkipUnchangedFiles skips files whose mtime+size haven't
                      changed since last scan
                    type: boolean
                type: object
              maxConcurrent:
                default: 5
                description: MaxConcurrent files to scan in parallel
                format: int32
                maximum: 20
                minimum: 1
                type: integer
              maxFileSize:
                default: 104857600
                description: MaxFileSize in bytes — files larger than this will be
                  skipped
                format: int64
                type: integer
              nodeName:
                description: NodeName is the name of the node to scan
                minLength: 1
                type: string
              paths:
                description: Paths to scan on the node
                items:
                  type: string
                type: array
              priority:
                default: medium
                description: Priority of the scan (high, medium, low)
                enum:
                - high
                - medium
                - low
                type: string
              resources:
                description: Resources for the scan job
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              scanPolicy:
                description: ScanPolicy references a ScanPolicy to use for this scan
                type: string
              strategy:
                allOf:
                - enum:
                  - full
                  - incremental
                  - modified-only
                  - smart
                - enum:
                  - full
                  - incremental
                  - modified-only
                  - smart
                default: full
                description: Strategy defines the scan strategy to use
                type: string
              ttlSecondsAfterFinished:
                default: 86400
                description: TTLSecondsAfterFinished limits the lifetime of a finished
                  Job
                format: int32
                type: integer
            required:
            - nodeName
            type: object
          status:
            description: NodeScanStatus defines the observed state of NodeScan
            properties:
              cacheHitRate:
                format: int32
                maximum: 100
                minimum: 0
                type: integer
              completionTime:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              duration:
                format: int64
                type: integer
              errorCount:
                format: int64
                type: integer
              exitCode:
                format: int32
                type: integer
              failureReason:
                type: string
              filesInfected:
                format: int64
                type: integer
              filesScanned:
                format: int64
                type: integer
              filesSkipped:
                format: int64
                type: integer
              filesSkippedIncremental:
                format: int64
                type: integer
              infectedFiles:
                description: Limited to first 100 for performance
                items:
                  description: InfectedFile represents a file found to be infected
                    with malware
                  properties:
                    detectedAt:
                      format: date-time
                      type: string
                    path:
                      type: string
                    size:
                      format: int64
                      type: integer
                    viruses:
                      items:
                        type: string
                      type: array
                  required:
                  - path
                  - viruses
                  type: object
                type: array
              infectedFilesTruncated:
                description: |-
                  InfectedFilesTruncated is true when the scan detected more than 100 infected
                  files. Only the first 100 are stored in InfectedFiles; the full count is
                  still reflected in FilesInfected. Always check this flag before drawing
                  security conclusions from InfectedFiles.
                type: boolean
              jobRef:
                description: ObjectReference contains enough information to let you
                  inspect or modify the referred object.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              lastTransitionTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: NodeScanPhase represents the current phase of a NodeScan
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                type: string
              reportPath:
                type: string
              resultsPartial:
                description: |-
                  ResultsPartial is true when scan result parsing failed after all retries
                  and the status reflects incomplete data. The scan itself succeeded (the
                  scanner Job exited 0) but the controller could not fully parse the output.
                  Do NOT treat this scan as a clean bill of health — re-run the scan.
                type: boolean
              scannerCPUUserMilliseconds:
                description: |-
                  ScannerCPUUserMilliseconds is the user-space CPU time (milliseconds) consumed
                  by the scanner Node.js process during the scan, as reported by
                  process.cpuUsage(). Stored as int64 milliseconds to avoid float serialization
                  issues in CRD schemas.
                format: int64
                type: integer
              scannerMemoryRSSBytes:
                format: int64
                type: integer
              startTime:
                format: date-time
                type: string
              strategyUsed:
                description: ScanStrategy defines the strategy to use for scanning
                enum:
                - full
                - incremental
                - modified-only
                - smart
                type: string
              timeSaved:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
