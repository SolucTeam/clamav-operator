apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: clusterscans.clamav.io
  annotations:
    "helm.sh/resource-policy": keep
    controller-gen.kubebuilder.io/version: v0.14.0
  labels:
    app.kubernetes.io/managed-by: Helm
spec:
  group: clamav.io
  names:
    kind: ClusterScan
    listKind: ClusterScanList
    plural: clusterscans
    shortNames:
    - cs
    - clusterscan
    singular: clusterscan
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.totalNodes
      name: Total
      type: integer
    - jsonPath: .status.completedNodes
      name: Completed
      type: integer
    - jsonPath: .status.failedNodes
      name: Failed
      type: integer
    - jsonPath: .status.totalFilesInfected
      name: Infected
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ClusterScan is the Schema for the clusterscans API (v1beta1 — stable, preferred version)
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: ClusterScanSpec defines the desired state of ClusterScan
            properties:
              concurrent:
                default: 3
                description: Concurrent is the maximum number of nodes to scan in parallel
                format: int32
                maximum: 50
                minimum: 1
                type: integer
              nodeSelector:
                description: NodeSelector selects which nodes to scan
                properties:
                  matchExpressions:
                    items:
                      properties:
                        key:
                          type: string
                        operator:
                          type: string
                        values:
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              priority:
                default: medium
                description: Priority of all scans in this cluster scan
                enum:
                - high
                - medium
                - low
                type: string
              scanPolicy:
                description: ScanPolicy references a ScanPolicy to use for all node scans
                type: string
              nodeScanTemplate:
                description: NodeScanTemplate contains the template for creating NodeScans
                properties:
                  scanPolicy:
                    type: string
                  priority:
                    default: medium
                    enum:
                    - high
                    - medium
                    - low
                    type: string
                  paths:
                    items:
                      type: string
                    type: array
                  excludePatterns:
                    items:
                      type: string
                    type: array
                  maxConcurrent:
                    default: 5
                    format: int32
                    maximum: 20
                    minimum: 1
                    type: integer
                  fileTimeout:
                    default: 300000
                    format: int64
                    type: integer
                  maxFileSize:
                    default: 104857600
                    format: int64
                    type: integer
                  resources:
                    properties:
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        type: object
                    type: object
                  ttlSecondsAfterFinished:
                    default: 86400
                    format: int32
                    type: integer
                  strategy:
                    description: Strategy defines the scan strategy to use for each NodeScan
                    enum:
                    - full
                    - incremental
                    - modified-only
                    - smart
                    type: string
                  incrementalConfig:
                    description: IncrementalConfig configures incremental scan behavior
                    properties:
                      fullScanInterval:
                        default: 10
                        description: FullScanInterval defines how often a full scan runs when strategy is "smart"
                        format: int32
                        type: integer
                      maxFileAgeHours:
                        default: 168
                        description: MaxFileAgeHours is the maximum file age (in hours) to consider
                        format: int32
                        type: integer
                      skipUnchangedFiles:
                        default: true
                        description: SkipUnchangedFiles skips files whose mtime+size haven't changed
                        type: boolean
                    type: object
                  forceFullScan:
                    description: ForceFullScan forces a full scan on each node even if incremental is configured
                    type: boolean
                type: object
            type: object
          status:
            description: ClusterScanStatus defines the observed state of ClusterScan (v1beta1)
            properties:
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed by the controller
                format: int64
                type: integer
              phase:
                description: Phase of the cluster scan
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                - PartiallyCompleted
                type: string
              startTime:
                description: StartTime of the cluster scan
                format: date-time
                type: string
              completionTime:
                description: CompletionTime of the cluster scan
                format: date-time
                type: string
              totalNodes:
                description: TotalNodes is the total number of nodes to scan
                format: int32
                type: integer
              completedNodes:
                description: CompletedNodes is the number of nodes that have completed scanning
                format: int32
                type: integer
              runningNodes:
                description: RunningNodes is the number of nodes currently being scanned
                format: int32
                type: integer
              failedNodes:
                description: FailedNodes is the number of nodes that failed to scan
                format: int32
                type: integer
              infectedNodes:
                description: InfectedNodes is the number of nodes with infected files
                format: int32
                type: integer
              totalFilesScanned:
                description: TotalFilesScanned across all nodes
                format: int64
                type: integer
              totalFilesInfected:
                description: TotalFilesInfected across all nodes
                format: int64
                type: integer
              nodeScans:
                description: NodeScans contains references to individual node scans
                items:
                  properties:
                    name:
                      type: string
                    nodeName:
                      type: string
                    phase:
                      enum:
                      - Pending
                      - Running
                      - Completed
                      - Failed
                      type: string
                    filesInfected:
                      format: int64
                      type: integer
                    filesScanned:
                      format: int64
                      type: integer
                    startTime:
                      format: date-time
                      type: string
                    completionTime:
                      format: date-time
                      type: string
                  required:
                  - name
                  - nodeName
                  - phase
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest available observations
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.totalNodes
      name: Total
      type: integer
    - jsonPath: .status.completedNodes
      name: Completed
      type: integer
    - jsonPath: .status.runningNodes
      name: Running
      type: integer
    - jsonPath: .status.failedNodes
      name: Failed
      type: integer
    - jsonPath: .status.totalFilesInfected
      name: Infected
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterScan is the Schema for the clusterscans API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterScanSpec defines the desired state of ClusterScan
            properties:
              concurrent:
                default: 3
                description: Concurrent is the maximum number of nodes to scan in
                  parallel
                format: int32
                maximum: 50
                minimum: 1
                type: integer
              nodeScanTemplate:
                description: NodeScanTemplate contains the template for creating NodeScans
                properties:
                  excludePatterns:
                    description: ExcludePatterns are regex patterns for paths to exclude
                    items:
                      type: string
                    type: array
                  fileTimeout:
                    default: 300000
                    description: FileTimeout in milliseconds for scanning each file
                    format: int64
                    type: integer
                  maxConcurrent:
                    default: 5
                    description: MaxConcurrent files to scan in parallel
                    format: int32
                    maximum: 20
                    minimum: 1
                    type: integer
                  maxFileSize:
                    default: 104857600
                    description: MaxFileSize in bytes - files larger than this will
                      be skipped
                    format: int64
                    type: integer
                  nodeName:
                    description: NodeName is the name of the node to scan
                    minLength: 1
                    type: string
                  paths:
                    description: |-
                      Paths to scan on the node
                      If not specified, uses paths from ScanPolicy or defaults
                    items:
                      type: string
                    type: array
                  priority:
                    default: medium
                    description: |-
                      Priority of the scan (high, medium, low)
                      Affects scheduling and resource allocation
                    enum:
                    - high
                    - medium
                    - low
                    type: string
                  resources:
                    description: Resources for the scan job
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  scanPolicy:
                    description: |-
                      ScanPolicy references a ScanPolicy to use for this scan
                      If not specified, default scan parameters will be used
                    type: string
                  ttlSecondsAfterFinished:
                    default: 86400
                    description: |-
                      TTLSecondsAfterFinished limits the lifetime of a Job that has finished
                      execution (either Complete or Failed). If this field is set,
                      ttlSecondsAfterFinished after the Job finishes, it is eligible to be
                      automatically deleted.
                    format: int32
                    type: integer
                  strategy:
                    description: |-
                      Strategy defines the scan strategy to use for each NodeScan.
                      full: scan every file on every run (default).
                      incremental: scan only files modified since the last scan.
                      modified-only: scan only files modified in the last 24 h.
                      smart: alternate automatically between incremental and full scans.
                    enum:
                    - full
                    - incremental
                    - modified-only
                    - smart
                    type: string
                  incrementalConfig:
                    description: IncrementalConfig configures incremental scan behavior
                    properties:
                      enabled:
                        default: false
                        description: Enabled activates incremental scanning
                        type: boolean
                      strategy:
                        default: incremental
                        description: Strategy defines the scan strategy within this config
                        enum:
                        - full
                        - incremental
                        - modified-only
                        - smart
                        type: string
                      baselineInterval:
                        default: 7
                        description: |-
                          BaselineInterval triggers a full scan every N incremental scans.
                          For example, setting 7 forces a full scan every 7 incremental scans.
                        format: int32
                        maximum: 30
                        minimum: 1
                        type: integer
                      maxAge:
                        default: 168
                        description: MaxAge is the maximum file age (in hours) to consider for incremental scans
                        format: int32
                        type: integer
                      minTimeBetweenScans:
                        default: 6
                        description: MinTimeBetweenScans is the minimum delay (in hours) between two scans
                        format: int32
                        type: integer
                      cacheExpiration:
                        default: 168
                        description: CacheExpiration is the cache validity duration (in hours); after this a full scan is forced
                        format: int32
                        type: integer
                      skipUnchangedFiles:
                        default: true
                        description: SkipUnchangedFiles skips files whose mtime+size have not changed since the last scan
                        type: boolean
                    type: object
                  forceFullScan:
                    description: ForceFullScan forces a full scan on each node even if an incremental strategy is configured
                    type: boolean
                type: object
              nodeSelector:
                description: |-
                  NodeSelector selects which nodes to scan
                  If not specified, all nodes will be scanned
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              priority:
                default: medium
                description: Priority of all scans in this cluster scan
                enum:
                - high
                - medium
                - low
                type: string
              scanPolicy:
                description: ScanPolicy references a ScanPolicy to use for all node
                  scans
                type: string
            type: object
          status:
            description: ClusterScanStatus defines the observed state of ClusterScan
            properties:
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent generation observed by the controller.
                  Used by ArgoCD, Flux and other GitOps tools to detect whether the controller
                  has processed the latest version of the spec.
                format: int64
                type: integer
              completedNodes:
                description: CompletedNodes is the number of nodes that have completed
                  scanning
                format: int32
                type: integer
              completionTime:
                description: CompletionTime of the cluster scan
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedNodes:
                description: FailedNodes is the number of nodes that failed to scan
                format: int32
                type: integer
              infectedNodes:
                description: InfectedNodes is the number of nodes with infected files
                format: int32
                type: integer
              nodeScans:
                description: NodeScans contains references to individual node scans
                items:
                  description: NodeScanReference references a NodeScan and its status
                  properties:
                    completionTime:
                      description: CompletionTime of the node scan
                      format: date-time
                      type: string
                    filesInfected:
                      description: FilesInfected on this node
                      format: int64
                      type: integer
                    filesScanned:
                      description: FilesScanned on this node
                      format: int64
                      type: integer
                    name:
                      description: Name of the NodeScan
                      type: string
                    nodeName:
                      description: NodeName is the node being scanned
                      type: string
                    phase:
                      description: Phase of the NodeScan
                      enum:
                      - Pending
                      - Running
                      - Completed
                      - Failed
                      type: string
                    startTime:
                      description: StartTime of the node scan
                      format: date-time
                      type: string
                  required:
                  - name
                  - nodeName
                  - phase
                  type: object
                type: array
              phase:
                description: Phase of the cluster scan
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                - PartiallyCompleted
                type: string
              runningNodes:
                description: RunningNodes is the number of nodes currently being scanned
                format: int32
                type: integer
              startTime:
                description: StartTime of the cluster scan
                format: date-time
                type: string
              totalFilesInfected:
                description: TotalFilesInfected across all nodes
                format: int64
                type: integer
              totalFilesScanned:
                description: TotalFilesScanned across all nodes
                format: int64
                type: integer
              totalNodes:
                description: TotalNodes is the total number of nodes to scan
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
